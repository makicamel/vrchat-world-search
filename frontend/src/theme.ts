import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    divider: '#fff',
    background: {
      default: '#1b2025',
    },
    text: {
      primary: '#fff',
      secondary: '#fff',
    },
  }
})


export default theme;
